#pragma GCC visibility push(hidden)
bool geomVectorCheckSize(const VECTOR& a,const VECTOR& b);    //kiểm tra 2 vector có cùng kích thước
bool geomVectorCheckZero(const VECTOR& a);                    //kiểm tra vector có phải vector 0
#pragma GCC visibility pop
